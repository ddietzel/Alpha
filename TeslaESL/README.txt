There should be an error to handle this, but it’s an edge case so I’m just noting it here:

This must be run on some kind of Webserver, otherwise you will get cross domain errors. If there’s none immediately available just take a look at it at:

www.danieldietzel.com/alpha/Tesla/